'use strict'
const turn = [0, 1, 2, 3, 4, 5, 6, 7, 8]

let cells = ['', '', '', '', '', '', '', '', '']

let game = function () {
  for (let i = 0; i < turn.length; i++) {
    if (turn[i] % 2 === 0) {

    } else {
    }
  }
}
